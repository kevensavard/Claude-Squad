{
  "name": "@squad/party",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "partykit dev",
    "deploy": "partykit deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "partykit": "0.0.115",
    "partysocket": "^1.0.2",
    "zod": "^3.22.0",
    "@squad/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^2.0.0"
  }
}
